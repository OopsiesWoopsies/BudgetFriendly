import db from '../db.js';
import { ipcMain } from 'electron';
import { enqueue } from '../dbQueue.js';

export function registerBudgetSettingsIpc() {
  // Get all categories
  // Insert into table (categories, title)
  // Delete category
}
